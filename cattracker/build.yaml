---
# Docker Hub (registry-1.docker.io) — many networks block ghcr.io; without a pull,
# the add-on looks “stuck” and nothing downloads. Image is Debian bookworm (apt-get works).
build_from:
  aarch64: python:3.11-slim-bookworm
  amd64: python:3.11-slim-bookworm
args: {}
